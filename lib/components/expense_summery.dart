import 'package:flutter/material.dart';

class ExpenseSummery extends StatelessWidget {
  const ExpenseSummery({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
